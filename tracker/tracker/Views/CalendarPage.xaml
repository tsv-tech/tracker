<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
	x:Class="tracker.Views.CalendarPage"
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:controls="clr-namespace:Xamarin.Plugin.Calendar.Controls;assembly=Xamarin.Plugin.Calendar">

	<ContentPage.ToolbarItems>
		<ToolbarItem
			x:Name="lblDay"
			Order="Primary"
			Priority="0"
			Text="" />
		<ToolbarItem
			Command="{Binding ExportTappedCommand}"
			Order="Primary"
			Priority="0"
			Text="EXPORT" />
	</ContentPage.ToolbarItems>

	<ContentPage.Content>
		<StackLayout>
			<controls:Calendar DayTappedCommand="{Binding DayTappedCommand}" Events="{Binding Days}">
				<controls:Calendar.EventTemplate>
					<DataTemplate>
						<StackLayout Padding="10,15,10,0" Orientation="Horizontal">
							<StackLayout>
								<StackLayout Orientation="Horizontal">
									<Label FontSize="Large" Text="Time:" />
									<Label FontSize="Large" Text="{Binding}" />

								</StackLayout>
							</StackLayout>
							<Label
								FontAttributes="Bold"
								FontSize="Large"
								HorizontalOptions="EndAndExpand"
								Text="{Binding GetDuration}" />
						</StackLayout>
					</DataTemplate>
				</controls:Calendar.EventTemplate>
			</controls:Calendar>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>